const INTIAL_STATE = {
  quote: "testing quote :)",
  isLoading: false,
};

const quotReducer = (state = INTIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default quotReducer;
